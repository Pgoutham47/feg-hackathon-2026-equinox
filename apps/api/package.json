{
  "name": "@eog/api",
  "version": "0.1.0",
  "private": true,
  "description": "Thin pnpm wrapper so Turborepo can drive the Python service alongside the web app. All real dependency management is uv + pyproject.toml.",
  "scripts": {
    "dev": "uv run uvicorn app.main:app --reload --port 8000",
    "lint": "uv run ruff check . && uv run ruff format --check . && uv run mypy app",
    "test": "uv run pytest",
    "migrate": "uv run alembic upgrade head",
    "seed": "uv run python scripts/seed.py"
  }
}
