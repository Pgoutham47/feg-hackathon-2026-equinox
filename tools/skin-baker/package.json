{
  "name": "@eog/skin-baker",
  "version": "0.1.0",
  "private": true,
  "description": "pnpm wrapper so Turborepo can drive the Python baker. Dependencies are uv + pyproject.toml.",
  "scripts": {
    "lint": "uv run ruff check . && uv run ruff format --check . && uv run mypy skin_baker",
    "test": "uv run pytest",
    "classify": "uv run skin-baker classify",
    "bake": "uv run skin-baker bake",
    "publish:local": "uv run skin-baker publish --backend local --local-root ../../cdn --no-dry-run"
  }
}
